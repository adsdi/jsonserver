{
  "homepage": "http://adsdi.github.io/jsonserver",
  "name": "jsonserver",
  "version": "1.0.0",
  "description": "Mock API",
  "main": "index.js",
  "scripts": {
    "json:server": "json-server --watch db.json",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "author": "Stephen Bolt",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.16.3"
  },
  "devDependencies": {
    "gh-pages": "^3.1.0"
  }
}
